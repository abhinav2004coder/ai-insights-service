version: '3.8'

services:
  ai-insights:
    build: .
    ports:
      - "8000:8000"
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - ENVIRONMENT=production
    env_file:
      - .env
    volumes:
      - ./models:/app/models
    restart: unless-stopped
